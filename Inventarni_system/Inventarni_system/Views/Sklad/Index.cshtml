@model IEnumerable<Inventarni_system.Models.Sklad>

@{
    ViewData["Title"] = "Sklady";
}

<h2>Sklady</h2>

<div class="mb-3">
    <a asp-action="Create" class="btn btn-success">Přidat nový sklad</a>
</div>

<form method="get" asp-controller="Sklad" asp-action="Index">
    <div class="form-group">
        <input type="text" name="searchString" value="@ViewData["CurrentFilter"]" class="form-control" placeholder="Hledat sklad...">
        <button type="submit" class="btn btn-primary mt-2">Vyhledat</button>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>Název</th>
            <th>Akce</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nazev</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Upravit</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Smazat</a>
                </td>
            </tr>
        }
    </tbody>
</table>
